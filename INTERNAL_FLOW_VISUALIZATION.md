# Agent 内部流程可视化 — 需求与开发计划

## 项目目标

帮助开发者理解 Agent 的工作原理和内部流程，将关键的决策过程、循环模式、状态变化可视化，让人一眼就能看懂 Agent 是怎么工作的。

---

## 一、核心可视化需求

### 1. Agent 决策循环可视化（ReAct Loop）

**目标**：展示 Agent 每次 LLM 调用的完整决策循环

**可视化内容**：
```
┌─────────────────────────────────────────────────┐
│  Loop #1 (2.3s)                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │  Think   │→ │  Tool    │→ │  Observation │   │
│  │  思考    │  │  Call    │  │  工具结果    │   │
│  └──────────┘  └──────────┘  └──────────────┘   │
│     ↓                                            │
│  选择了 web_search 工具                           │
│  原因: 用户问了一个需要实时信息的问题              │
└─────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────┐
│  Loop #2 (1.8s)                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │  Think   │→ │  Tool    │→ │  Observation │   │
│  │  思考    │  │  Call    │  │  工具结果    │   │
│  └──────────┘  └──────────┘  └──────────────┘   │
│     ↓                                            │
│  选择了 file_read 工具                            │
│  原因: 需要读取本地文件内容                       │
└─────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────┐
│  Loop #3 (0.9s)                                  │
│  ┌──────────┐                                    │
│  │  Think   │→  直接回答（无工具调用）             │
│  │  思考    │  原因: 已有足够信息生成回答          │
│  └──────────┘                                    │
└─────────────────────────────────────────────────┘
```

**关键指标**：
- 循环次数
- 每次循环耗时
- 循环类型（有工具调用 vs 直接回答）
- 工具选择的原因推断

---

### 2. 工具调用决策树

**目标**：展示 Agent 为什么选择某个工具，以及工具之间的依赖关系

**可视化内容**：
```
用户问题: "帮我查一下今天的天气，然后写到文件里"
                    │
              ┌─────┴─────┐
              │  决策分析  │
              └─────┬─────┘
                    │
        ┌───────────┼───────────┐
        ▼           ▼           ▼
   web_search   file_write   (其他)
   (先查天气)   (后写文件)
        │           │
        └─────┬─────┘
              ▼
         任务完成
```

**关键信息**：
- 工具调用顺序
- 工具间的依赖关系（前一个工具的输出是后一个的输入）
- 工具选择的原因（从 prompt/context 推断）

---

### 3. Context Window 变化过程

**目标**：展示随着对话进行，Context Window 是如何变化的

**可视化内容**：
```
┌──────────────────────────────────────────────┐
│  Token 使用量随时间变化                        │
│                                              │
│  8000 ┤                                      │
│       │         ╭────╮                       │
│  6000 ┤        ╱      ╲                      │
│       │       ╱        ╲                     │
│  4000 ┤      ╱          ╲                    │
│       │     ╱            ╲                   │
│  2000 ┤────╯              ╲                  │
│       │                     ╲                │
│     0 ┼─────┬─────┬─────┬─────┬─────         │
│          L1    L2    L3    L4    L5          │
│                                              │
│  L1: 初始 prompt (2.1k tokens)               │
│  L2: + 用户问题 + 工具结果 (4.3k)            │
│  L3: + 更多上下文 (6.8k)                     │
│  L4: 触发压缩 → 降到 3.2k                    │
│  L5: 继续增长 (5.1k)                         │
└──────────────────────────────────────────────┘
```

**关键信息**：
- 每次 LLM 调用时的 token 使用量
- 上下文压缩的触发点和效果
- system prompt、tools 定义、messages 各占多少 token

---

### 4. Agent 状态机视图

**目标**：展示 Agent 在整个生命周期中的状态变化

**可视化内容**：
```
┌─────────┐    ┌──────────┐    ┌──────────┐    ┌─────────┐
│  Idle   │───→│ Thinking │───→│ Executing│───→│  Idle   │
│  空闲   │    │  思考中  │    │  执行中  │    │  空闲   │
└─────────┘    └──────────┘    └──────────┘    └─────────┘
     ↑                │                              │
     │                ↓                              │
     │          ┌──────────┐                         │
     └──────────│ Waiting  │←────────────────────────┘
                │  等待中  │
                └──────────┘
```

**状态转换记录**：
```
时间线:
00:00  Idle → Thinking    (收到用户消息)
00:02  Thinking → Executing (决定调用 web_search)
00:05  Executing → Thinking (工具返回结果)
00:07  Thinking → Executing (决定调用 file_write)
00:08  Executing → Thinking (工具返回结果)
00:09  Thinking → Idle      (生成最终回答)
```

---

### 5. 错误与重试可视化

**目标**：展示 Agent 遇到错误时如何处理和重试

**可视化内容**：
```
┌─────────────────────────────────────────────────┐
│  ⚠️ 错误处理过程                                 │
│                                                  │
│  Tool: web_search                                │
│  第1次: ❌ 超时 (5s)                              │
│  第2次: ❌ 连接失败                               │
│  第3次: ✅ 成功返回结果                           │
│                                                  │
│  Agent 反应: "搜索服务暂时不可用，重试中..."       │
└─────────────────────────────────────────────────┘
```

---

### 6. 多 Session 对比（可选）

**目标**：对比同一个 Agent 在不同场景下的行为差异

**对比维度**：
- 循环次数
- 工具调用顺序
- 总耗时
- Token 消耗

---

## 二、数据来源

| 数据 | 来源 | 说明 |
|------|------|------|
| LLM 请求/响应 | viz-proxy | 包含完整的 messages、tools、usage |
| 工具调用 | viz-proxy + plugin hooks | 工具名称、参数、结果、耗时 |
| 状态变化 | plugin hooks | 基于 hooks 推断的状态 |
| 上下文压缩 | plugin hooks | 压缩前后的 token 数 |

---

## 三、开发计划

### Phase 1: 决策循环可视化（核心）

**目标**：让用户一眼看到 Agent 的 ReAct 循环过程

**任务**：
1. 从 proxy 数据中识别循环边界（每次 LLM 调用 = 一个循环）
2. 提取每次循环的关键信息：
   - 思考内容（assistant message）
   - 工具调用（tool_calls）
   - 工具结果（tool results）
3. 前端新增"循环视图"Tab
4. 用 React Flow 或时间线组件展示循环流程
5. 标注每次循环的耗时和类型

**预计改动**：
- `viz-frontend/src/components/`: 新增 `ReActLoopView.tsx`
- `viz-backend/server.js`: 新增循环分析 API
- `viz-proxy/server.js`: 无需改动（数据已足够）

---

### Phase 2: 工具调用决策树

**目标**：展示工具调用的顺序和依赖关系

**任务**：
1. 分析工具调用序列，识别依赖关系
2. 前端用树形图或流程图展示
3. 标注工具选择的原因（从 context 推断）

**预计改动**：
- `viz-frontend/src/components/`: 新增 `ToolDecisionTree.tsx`
- 后端新增依赖分析逻辑

---

### Phase 3: Context Window 变化图

**目标**：展示 token 使用量的变化趋势

**任务**：
1. 从 proxy response 中提取 `usage` 字段
2. 前端用折线图展示 token 变化
3. 标注上下文压缩事件

**预计改动**：
- `viz-frontend/src/components/`: 新增 `TokenUsageChart.tsx`
- 后端存储 usage 数据

---

### Phase 4: 状态机视图 + 错误追踪

**目标**：展示 Agent 状态变化和错误处理

**任务**：
1. 前端新增状态机视图
2. 错误事件高亮展示
3. 重试过程可视化

**预计改动**：
- `viz-frontend/src/components/`: 新增 `StateMachineView.tsx`
- 后端接收 error 事件

---

## 四、优先级建议

| 优先级 | 功能 | 价值 | 难度 |
|--------|------|------|------|
| P0 | 决策循环可视化 | ⭐⭐⭐⭐⭐ | 中 |
| P0 | Token 使用量图表 | ⭐⭐⭐⭐ | 低 |
| P1 | 工具调用决策树 | ⭐⭐⭐⭐ | 中 |
| P1 | 状态机视图 | ⭐⭐⭐ | 低 |
| P2 | 错误与重试可视化 | ⭐⭐⭐ | 低 |
| P2 | Context Window 组成分析 | ⭐⭐⭐ | 中 |
| P3 | 多 Session 对比 | ⭐⭐ | 高 |

---

## 五、技术选型建议

| 功能 | 推荐组件 |
|------|----------|
| 循环流程图 | React Flow 或自定义时间线 |
| Token 趋势图 | Recharts / Chart.js |
| 工具决策树 | React Flow |
| 状态机视图 | 自定义 SVG 或 React Flow |
| 错误高亮 | 现有 Timeline 组件扩展 |

---

*创建时间: 2026-06-07*
